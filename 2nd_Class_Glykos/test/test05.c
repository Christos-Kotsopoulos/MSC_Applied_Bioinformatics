#include <stdio.h>
#include <math.h>
#include <string.h>
int main()
{

int length, i;

	char seq [ 1000 ];
while ( scanf("%s", seq ) == 1)
{
	length = strlen( seq );
	printf("The lenght of your string is %d\n", length );

      }

}

